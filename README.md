# Fastly Logging
An HTTP server to print Fastly logs.

### Pre-Requirements
* Docker / npm 
### How to install

#### Docker
```shell
docker run -d --name fastly-logging guyeise5/fastly-logging
```

#### npm
```shell
npm install
npm start
```